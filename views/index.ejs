<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="index.css">
</head>

<body>
    <div class="container-fluid">
        <h2>Medium</h2>
        <hr>
        <p>Discover Stories, Articles and Research Work from GLA Students.</p>
    </div>

    <a href="http://localhost:3000/new"><button class="btn btn-primary sp">Add New Article</button></a>

    <ul>
        <% for(const arti of articles){ %>
            <li>
                <div class="card mt-4">
                    <div class="card-body">
                        <h4 class="card-title">
                            <%= arti.title %>
                        </h4>
                        <div class="card-subtitle text-muted mb-2">
                            <%=arti.createdAt.getDate()%>/
                                <%=arti.createdAt.getMonth()%>/
                                    <%=arti.createdAt.getFullYear()%>
                        </div>
                        <div class="card-text mb-2">
                            <%= arti.description %>
                        </div>
                        <div id="bt">
                            <a href="http://localhost:3000/<%=arti.id%>"><button class="btn btn-primary">View</button></a>
                            <a href="http://localhost:3000/<%=arti.id%>/edit"><button class="btn btn-success">Edit</button></a>
                            <form action="/<%=arti.id%>?_method=DELETE" method="post">
                                <button class="btn btn-danger" type="submit">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            </li>
            <%}%>
    </ul>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>